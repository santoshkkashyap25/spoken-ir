# API URL for the Streamlit frontend to connect to the FastAPI backend
TRANSNLP_API_URL=http://localhost:8000

# Optional: Number of worker threads for parallel scraping (default: 4)
# MAX_SCRAPE_WORKERS=4

# Optional: NLTK Data directory override
# NLTK_DATA=./data/nltk_data
