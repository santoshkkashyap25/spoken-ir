[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "spoken-ir"
version = "2.0.0"
description = "Stand-Up Comedy Semantic Search & Hybrid Retrieval Engine"
authors = [{name = "Spoken-IR"}]

[tool.setuptools.packages.find]
include = ["ai*", "backend*", "utils*"]
